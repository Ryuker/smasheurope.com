// New Optimized Event Filter 
const eventsEls = document.querySelectorAll('#events .card-title');

// const itemFilter = document.getElementById('filter');

// names.forEach(event => event.textContent);

// const events = await document.querySelectorAll('#events .card-title');
// console.dir(names);

document.addEventListener("DOMContentLoaded", function(){
  // Code here waits to run until the DOM is loaded.
  const eventsEls = document.querySelectorAll('#events .card-title');
  // console.dir(names.forEach(event => event.textContent), 'names');
});

function setup(){
  const eventsEls = document.querySelectorAll('#events .card-title');
}