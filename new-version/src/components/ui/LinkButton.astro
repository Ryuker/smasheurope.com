---
import { Icon } from 'astro-icon/components';
import Tooltip from './Tooltip.astro';
const {
  icon, 
  text, 
  url,
  id,
  tooltipText = '',
  classes = " ",
  colors = 'bg-se-blue-100 hover:bg-se-blue-200', 
  newTab = false
} = Astro.props;

---

<a target={newTab ? '_blank': ''} href={url} class={`${classes} ${colors} px-4 md:px-6 py-3 min-w-20 rounded-lg flex justify-center items-center`} id={`btn-${id}`} aria-describedby="tooltip">
  {icon && <Icon name={icon} class="text-lg" />}
  {text && text}
</a>

{ tooltipText && 
  <Tooltip text={tooltipText} buttonId={`btn-${id}`} />
}